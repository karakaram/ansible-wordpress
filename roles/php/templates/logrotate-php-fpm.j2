/var/log/php-fpm/*log {
    weekly
    missingok
    rotate 7
    notifempty
    create 664 nginx {{ user.name }}
    sharedscripts
    delaycompress
    postrotate
        /bin/kill -SIGUSR1 `cat /var/run/php-fpm/php-fpm.pid 2>/dev/null` 2>/dev/null || true
    endscript
}
